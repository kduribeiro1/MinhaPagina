@page "/projetos"
@inject HttpClient Http

<section class="section container">
    <h1>Portfólio</h1>
    <p class="h-sub">Alguns projetos e estudos que mostram meu estilo e stack.</p>
    <div class="grid">
        @if (projetos == null)
        {
            <p>Carregando projetos...</p>
        }
        else
        {
            @foreach (var projeto in projetos)
            {
                <ProjectCard Name="@projeto.Name" Summary="@projeto.Summary" Year="@projeto.Year" Link="@projeto.Link" Role="@projeto.Role" />
            }
        }
    </div>
</section>

@code {
    private List<Projeto>? projetos;

    protected override async Task OnInitializedAsync()
    {
        projetos = await Http.GetFromJsonAsync<List<Projeto>>("dados/projetos.json");
    }
}